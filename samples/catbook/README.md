# Catbook

A social network for cats.

This example application has been created by using a large language model.

It uses Teal (`*.tl`) for the backend, and React 19 in in `index.jsx` for the frontend.
