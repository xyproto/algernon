text/html
qwen2.5-coder:1.5b

Generate an example HTML document that includes this text: "<img src='circle.prompt' alt='A circle'>".
"circle.prompt" is an SVG image with the mimetype set by the server.
Use an HTML title.
Only output the raw HTML code.
