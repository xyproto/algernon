-- A simple Teal example that just defines a function and outputs the result

function add(a: number, b: number): number
  return a+b
end

print(add(40, 2))

-- Try this on a system that has an "ls" command in the PATH
--print(json(run("ls")))
