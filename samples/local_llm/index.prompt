text/html
qwen2.5-coder:1.5b

Generate an HTML document that includes "<img src='circle.prompt' alt='A circle'>" and has the title "Fun Circle".
Use CSS for animating the img so that it pulses.
Use CSS to make the HTML background look like a sunset.
Only output the raw HTML code, with inline CSS.
